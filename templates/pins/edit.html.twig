{% extends 'base.html.twig' %}

{% block title 'Edit pin #' ~ pin.id %}

{% block body %}
  <h1>Edit pin #{{ pin.id }}</h1>

  {{ form_start(monFormulaire) }}

    {{ form_widget(monFormulaire) }}

    <input type="submit" value="Update pin">
  {{ form_end(monFormulaire) }}


{% endblock %}
